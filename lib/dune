(library
 (name eehs)
 (public_name eehs)
 (foreign_stubs
  (language c)
  (names epoll_stubs)
  (flags :standard))
 (libraries unix cstruct base_bigstring))

(rule
 (target config.ml)
 (action
  (run ./configure/configure.exe)))
